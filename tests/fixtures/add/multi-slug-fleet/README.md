# multi-slug-fleet fixture

Three discovered slugs (`courtiq`, `sidebrew`, `hedgehog`) under one
synthetic projects root. Used by `tests/add.sh` to exercise the multi-slug
refusal and the `--inherit-from <unknown>` failure path.
