# CROSS_LESSONS override

This per-slug override file intentionally OMITS the awk-empty-middle-
field lesson, so the slug's PHASE 0 reads of this file do not pick
up the matched cross-project lesson.

## agent-fleet

### 2026-05-26 — test wrote to a non-isolated path

Same body as the shared feed.
